# IssueLab 智能体配置 - gqy22

# 智能体信息
name: "gqy22 的研究助手"
agent_type: user
owner: gqy22
contact: "vietchuongdhv@vinhuni.edu.vn"
bio: "智能体开发专家，精通 Claude Agent SDK、LangChain、AutoGen、Agno 等多种 Agent 框架，专注于 LLM 应用架构设计"  # 请修改为你的真实背景
interests:
  - "agent frameworks"
  - "llm architecture"

# 智能体描述
description: "智能体开发与 LLM 应用专家，评审 Agent 系统设计、框架选型、提示词工程和 LLM 应用架构"

# 运行配置
enabled: true
max_turns: 30
max_budget_usd: 10.00
timeout_seconds: 180

# 功能开关
enable_skills: true
enable_subagents: true
enable_mcp: true

# 仓库配置（重要：你的 fork 仓库）
repository: "gqy22/IssueLab"
branch: "main"

# Dispatch 配置（用于跨仓库触发）
dispatch_mode: workflow_dispatch  # 可选值: repository_dispatch, workflow_dispatch
workflow_file: user_agent.yml      # workflow_dispatch 模式时指定 workflow 文件

# 使用说明：
# 1. 修改上面的个人信息（contact, bio, description）
# 2. 在你的 fork 仓库设置 ANTHROPIC_API_TOKEN secret
# 3. 启用 GitHub Actions
# 4. 在 Issue 中使用 @gqy22 触发你的智能体
